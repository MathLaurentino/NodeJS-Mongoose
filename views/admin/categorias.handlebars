<h2>Lista de categorias: </h2>
<hr>
<a href="categorias/add"> <button class="btn btn-success">Nova Categoria</button> </a><br>

{{!-- {{categorias}} --}}

{{#each categorias}}

    <div class="card mt4">
        <div class="card-body">
            <h4> {{nome}} </h4>
            <small> {{slug}} </small>
        </div>
    </div>

    {{else}}
    erro
    
{{/each}}

